# ADHD Tasks - Environment Variables
# Copy this file to .env and fill in your actual values

# Node environment: development, test, or production
NODE_ENV=development

# Backend API port (default: 5000)
PORT=5000

# Frontend client URL for CORS configuration
# In development: http://localhost:5173 (Vite default)
# In production: your deployed frontend URL
CLIENT_URL=http://localhost:5173

# Database connection string for PostgreSQL
# Format: postgres://username:password@host:port/database
# Local example: postgres://postgres:password@localhost:5432/adhd_tasks
# Vercel Postgres is auto-configured if using their platform
DATABASE_URL=postgres://postgres:password@localhost:5432/adhd_tasks

# OpenAI API configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key
# Optional: OpenAI model to use (defaults to gpt-4-turbo if not specified)
OPENAI_MODEL=gpt-4-turbo

# Authentication
# JWT secret for signing tokens - use a strong random string
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-jwt-key-at-least-32-chars-long
# JWT expiration time (default: 7 days)
JWT_EXPIRES_IN=7d

# Optional: Email service for notifications (if implemented)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password

# Optional: Sentry error tracking (if implemented)
# SENTRY_DSN=https://your-sentry-dsn

# Optional: Redis for caching (if implemented)
# REDIS_URL=redis://localhost:6379
